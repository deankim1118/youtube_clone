mixin video(video)
    div.video-mixin
        div.video-mixin__thumb
        div.video-mixin__data
            a(href=`/videos/${video.id}`).video-mixin__title= video.title 
            div.video-mixin__meta 
                span #{video.owner.name} ❤️
                span #{video.meta.views} views